@model IEnumerable<skill.Models.SiteConfig>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.errorMailTo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.errorMailCC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.errorMailTo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.errorMailCC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SiteConfigId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SiteConfigId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SiteConfigId })
        </td>
    </tr>
}

</table>
