@model IEnumerable<skill.Models.CandidateLanguage>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RegistrationId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LanguageMstId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RegistrationId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LanguageMstId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CadidateLanguageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CadidateLanguageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CadidateLanguageId })
        </td>
    </tr>
}

</table>
